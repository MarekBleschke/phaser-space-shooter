{
  "scripts": {
    "build": "browserify src/main.js -o dist/game.js -t [ babelify --presets [ es2015 ] ]",
    "watch": "watchify src/main.js -o dist/game.js --debug --verbose -t [ babelify --presets [ es2015 ] ]"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.18.0",
    "babelify": "^7.3.0",
    "phaser": "^2.6.2"
  }
}
